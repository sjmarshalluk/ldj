
<%= simple_form_for [@user, @job] do |f| %>
	
	<%= f.input :title %>
	<%= f.input :company %>
	<%= f.input :company_url %>
	<%= f.input :job_url %>
	<%= f.input :email %>
	<%= f.input :description %>

	<%= f.submit %>

<% end %>